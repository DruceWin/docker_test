version: '3.7'

services:
#  db:
#    image: postgres:12.0-alpine
#    volumes:
#      - postgres_volume:/var/lib/postgresql/data/
#    enviroment:
#      - POSTGRES_USER=admin
#      - POSTGRES_PASSWORD=admin
#      - POSTGRES_DB=my_dj_ps_dock
  web:
    build: .
    command:
      - python
      - manage.py
      - runserver
      - 0.0.0.0:8000
    volumes:
      - .:/./app/
    ports:
      - 8000:8000


#volumes:
#  postgres_volume: